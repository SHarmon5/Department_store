<%= link_to 'Home', depts_path %>
<h1>Click an Item To View it</h1>
<%= link_to 'Create a new item', new_dept_item_path(@dept) %>
<ul>
  <% @items.each do |item| %>
    <li><%= link_to item.name, [@dept, item] %></li>

  <% end %>
</ul>

